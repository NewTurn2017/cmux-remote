// M3.1 placeholder. Real entry point lands in M3.12 (cmux-relay CLI +
// MenuBarUI via ArgumentParser); for now this exists only so SwiftPM's
// executableTarget has at least one source file and `swift build` succeeds.
//
// Once M3.12 lands, this file is replaced by the ArgumentParser-rooted
// `@main struct CmuxRelay: ParsableCommand`.
print("cmux-relay placeholder — milestone M3 in progress")
